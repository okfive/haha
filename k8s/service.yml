apiVersion: v1
kind: Service
metadata:
  name: vxdggkh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "vxdggkh"
    port: 8080
  selector:
    app: vxdggkh
